# Coldline - Task 1.4
# File: submission-sample.yaml
# Fictional shape example only; values do not answer the supplied case.
answers:
  # baseline_run_1: complete every named field below.
  baseline_run_1:
    # Number in the unit named in this field; absolute tolerance 0.001.
    window_seconds: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    accepted_count: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    api_acceptance_per_second: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    completed_count: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    worker_completions_per_second: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    mean_worker_service_seconds: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    mean_accepted_to_terminal_seconds: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    outstanding_count: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    provider_setting_ms: 7
  # baseline_run_2: complete every named field below.
  baseline_run_2:
    # Number in the unit named in this field; absolute tolerance 0.001.
    window_seconds: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    accepted_count: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    api_acceptance_per_second: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    completed_count: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    worker_completions_per_second: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    mean_worker_service_seconds: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    mean_accepted_to_terminal_seconds: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    outstanding_count: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    provider_setting_ms: 7
  # repeatability_comparison: complete every named field below.
  repeatability_comparison:
    # Number in the unit named in this field; absolute tolerance 0.01.
    baseline_acceptance_change_percent: 7
    # Number in the unit named in this field; absolute tolerance 0.01.
    baseline_completion_change_percent: 7
    # Number in the unit named in this field; absolute tolerance 0.01.
    baseline_service_change_percent: 7
    # Choose one: within-15-percent-for-three-compared-rates, outside-15-percent-for-at-least-one-compared-rate.
    repeatability: outside-15-percent-for-at-least-one-compared-rate
    # Number in the unit named in this field; absolute tolerance 0.01.
    injected_acceptance_change_percent: 7
    # Number in the unit named in this field; absolute tolerance 0.01.
    injected_completion_change_percent: 7
    # Number in the unit named in this field; absolute tolerance 0.01.
    injected_service_change_percent: 7
    # Choose one: decreased, increased, unchanged.
    completion_effect: unchanged
  # latency_injected_run: complete every named field below.
  latency_injected_run:
    # Number in the unit named in this field; absolute tolerance 0.001.
    window_seconds: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    accepted_count: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    api_acceptance_per_second: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    completed_count: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    worker_completions_per_second: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    mean_worker_service_seconds: 7
    # Number in the unit named in this field; absolute tolerance 0.001.
    mean_accepted_to_terminal_seconds: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    outstanding_count: 7
    # Integer count in the unit named in this field; no rounding tolerance.
    provider_setting_ms: 7
  # bottleneck_analysis: complete every named field below.
  bottleneck_analysis:
    # Choose one: sequential-worker-service, api-ingestion, redis-retained-entries.
    candidate: redis-retained-entries
    # Choose one: supported, contradicted, insufficient-evidence.
    assessment: insufficient-evidence
    # Choose 2-4 unique IDs/values (order independent): E41, E42, E43, E44, E45, E46.
    evidence_ids:
    - E45
    - E46
  # alternative_elimination: complete every named field below.
  alternative_elimination:
    # Choose one: provider-delay-contributes-to-worker-service-time, api-acceptance-rate-is-worker-throughput, redis-xlen-is-outstanding-work.
    claim: redis-xlen-is-outstanding-work
    # Choose one: supported, contradicted, insufficient-evidence.
    assessment: insufficient-evidence
    # Choose 2-4 unique IDs/values (order independent): E41, E42, E43, E44, E45, E46.
    evidence_ids:
    - E45
    - E46
    # Choose one: supported, contradicted, insufficient-evidence.
    production_provider_claim: insufficient-evidence
  # fidelity: complete every named field below.
  fidelity:
    # Choose one: bounded-local-evidence, production-capacity-guarantee.
    local_result: production-capacity-guarantee
    # Choose one: accepted-minus-terminal-at-common-cutoff, xlen, pending-only.
    queue_measurement: pending-only
    # Choose one: repeat-isolated-runs-and-observe-queue-and-service, add-api-and-worker-p95, infer-managed-aws-performance.
    next_measurement: infer-managed-aws-performance
